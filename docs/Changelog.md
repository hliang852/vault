# Changelog

Dated log of changes made to this repo and why. Newest entries first.

## 2026-07-12 (2) -- Two integration follow-up decisions resolved

- **`has_dual_antitrust_review`: kept consolidated** (maintainer decision). The original 62-case rationale (JFTC ⊂ DOJ/FTC exact subset) no longer holds under japan-ma's regulator recoding, but "both true" is now a genuinely rare (4/110) dual-review signal worth its 0.5 weight as one shared fact. Engine comment and `Japan_User_Guide.md` §2 updated to the new rationale; `has_JFTC`/`has_DOJ_FTC_HSR` remain individually usable, just not separately scored. No score/output change.
- **`japan-ma/data/transform.py`: synced** (maintainer decision) to be byte-identical with the canonical `japan-ma/pipeline/transform.py`. Verified before overwriting that the `data/` copy was a strict ancestor (no unique content: it lacked only the derived-features block and the xlsx writer), and after syncing that both copies produce identical `Japan.csv` output. Convention: edit only `pipeline/transform.py`, then re-copy to `data/`.

## 2026-07-12 -- Integration: japan-ma (Part 1) wired into Part 2/3 as one pipeline

The standalone research pipeline in `japan-ma/` (deep-research intake agent, daily scanner, `pipeline/transform.py`) is now the canonical Part 1. Its `japan-ma/data/Japan_Master.csv` (110 landmark cases, **2015–2026**, 100% primary-source cited) and generated `japan-ma/data/Japan.csv` supersede the old `data/` copies; `vault/data/` is retained as a frozen pre-integration archive (see `data/ARCHIVED.md`). Maintainer decisions this session: derived features live in Part 1's transform (not a Part 2 load step); old `data/` archived not deleted; automation is a one-command orchestrator + watch mode (no daemon, no CI coupling).

- **`japan-ma/pipeline/transform.py` extended with the 8 Part 2 derived feature columns** (`price_was_bumped_bool`, `multi_jurisdiction`, `notes_flags_precedent_setting`, the 3 `timeline_post_*` overlay flags, and the 2 consolidated pair-features), so the canonical `Japan.csv` (now 110 × 165) arrives analysis-complete. Rules mirror `docs/Japan_User_Guide.md` §2 exactly and were **verified to reproduce the archived 62-row file bit-for-bit from its own source columns** before porting; one real bug caught and fixed in the process (the transform encodes `price_was_bumped` as the *string* `"True"`, so an `is True` identity test silently produced all-False — now `str(v) == "True"`). On the 62 shared rows, every remaining old-vs-new difference traces to a source cell japan-ma's re-verification legitimately updated (0 unexplained).
- **All Part 2/3 code repointed** to `japan-ma/data/Japan.csv`: `src/explore.py`, `src/cluster/precedent_engine.py`, `viewer/build.py`, and `notebooks/exploratory_analysis.py`'s path candidates.
- **`run_pipeline.py` (new, repo root).** One command runs explore → engine → site build in order, stopping loudly on the first failure; `--transform` prepends the Part 1 transform; `--watch` polls the canonical CSV (sha256, default 15 s) and auto-runs the chain whenever Part 1 lands a new file — satisfying "receipt of a new Japan.csv automatically kickstarts Part 2".
- **Map layout scales automatically.** `viewer/build.py` now auto-places any node missing from `viewer/layout.json` (deterministic seed from `deal_id`, near its archetype's existing region, collision-relaxed against everything, clamped to the viewbox) and persists the result — the 62 approved positions are preserved bit-for-bit, the 48 new deals were placed with zero node overlaps.
- **Site copy is data-driven.** Case count, year span and the corner marquee are computed from the corpus at build time (`110`, `2015–2026`, "2015 FEB — 2026 MAR"); the finder's industry dropdown is now populated from real `industry_group` values (the old hardcoded list had gone stale against japan-ma's recoded vocabulary — e.g. `Financials (banking/insurance)` → `Financials` — and would have silently matched nothing). Also fixed a latent finder bug: the archetype `<select>` was read via `+value` (NaN for value-less options, so the archetype term was never applied in-browser) — now `selectedIndex`.
- **Corpus-shift notes for Part 2 review** (logged in `To-do.md`, not silently retuned): japan-ma's regulator recoding breaks the old `has_dual_antitrust_review` rationale (JFTC was an exact subset of DOJ/FTC in the 62-case corpus; now 49 JFTC-only / 8 DOJ-only / 4 dual), and `TOP_K`/`MIN_SCORE`/`COMMUNITY_MIN_SCORE` were tuned on n=62 (at n=110 the k-clique cross-check now yields 11 communities, sizes [16,6,4,4,4,4,4,3,3,3,3], 46/110 covered).
- End-to-end verified: `python run_pipeline.py` runs the full chain cleanly (110 nodes, 321 edges, 7 archetype clusters), embedded app passes `node --check`, no stale copy or tokens, all rows carry the derived features.

## 2026-07-11 (2) -- Part 3: finder v2 (activists + axis characteristics + any-combination cluster)

Extended the live finder to make fuller use of Part 2's features and to realise the "any combination still forms a cluster" goal. All additions are in the **Advanced options** panel and are purely additive — the scoring semantics are unchanged (unstated characteristics still contribute nothing, neither rewarded nor penalised; documented now in the on-page disclaimer too).

- **Named-activist selector.** Chips for the 14 named funds in the corpus (ordered by recurrence: Elliott ×7, ValueAct ×5, Oasis ×4, …). Selecting a fund adds the existing `ACTIVIST_MATCH_WEIGHT` (2.5 — the single strongest signal in the model) for every case naming it. Verified: "Elliott" alone returns exactly the 7 Elliott cases at 2.5 each.
- **Axis characteristics as buttons.** "Board stance" (the *consent* axis → `board_recommendation_code`, single-choice) and "Structure & price" (the *price-discovery* axis → the atomic `competing_bid_flag` / `structure_has_MBO` / `structure_has_scheme` / `structure_has_merger` features). **These map to the atomic scored features, not the composite axes** — Part 2 deliberately left `instigator_type` / `lockup_signal_count` / `activist_signature` unscored because they recombine already-weighted columns, and adding the composites here would double-count. Instigator/lock-up remain covered by the existing quick pills.
- **Archetype "any".** The archetype dropdown gained a "deal of no particular type" option → no `category_group` term is added, so a query can be driven purely by the chosen characteristics rather than forced into one family.
- **Any combination → ad-hoc similarity cluster.** Below the ranked results, the top matches now render as a floating constellation drawing the **real Part 2 similarity edges among them** — so a combination that isn't a pre-built family (e.g. "Elliott": 7 cases across 3 archetype families, 12 shared-fact edges; "Opposed board + competing bid": 8 cases across 4 families, 6 edges) still appears as a related, neighboured group, with a caption stating how many families it spans and how many links connect them (or noting when matches are independent with no direct link).
- Still a precedent index, not a prediction; every match reason (including which activist) is shown. Verified `node --check` on the embedded app and mirrored all new scoring paths in Python against the engine weights.

## 2026-07-11 -- Part 3: production site built (`viewer/index.html` + `viewer/build.py`)

Turned the approved design mockup into a real, data-driven site generated from Part 2's output. This is the first Part 3 surface that reads its data directly from `output/precedent_graph_data.json` rather than a hand-embedded blob — regenerating the graph and re-running the build now flows straight through to the site.

- **`viewer/build.py` (new) — the build step.** Reads `output/precedent_graph_data.json` (Part 2 nodes/edges/features/axes/weights/communities), `data/Japan.csv` (tickers as `xxxx JP Equity`, dates, prices with their `_is_estimate` flags preserved, narrative dossier fields), `viewer/layout.json` (the committed 2-D arrangement — positions only), and any `content/cases/<deal_id>.md` plug-ins, then injects one `DATA` object into `viewer/template.html` at its `/*__DATA__*/` marker and writes `viewer/index.html`. Node radius is recomputed from real `deal_size_usd_mn` (sqrt scale); centroids are recomputed from the positions. The content loader validates per `Case_Content_Spec.md` and fails loudly (deal_id/filename mismatch, unknown id, non-https URL, bad source type); with no `.md` files present today, all dossiers render their "pending" slots. No new facts introduced — every field is a copy or mechanical transform of an existing source cell, per `Claude.md`.
- **`viewer/template.html` (new) — the generalised page.** Same approved Japanese-minimal design as mockup v6, but the three hardcoded demo flows are now data-driven: **Layer 2** `renderCluster(cg)` renders any of the 7 `category_group` clusters (real outcome split, recurring characteristics from `axes` frequencies, each case's resolution from its mechanical `brief`); **Layer 3** `renderCase(id)` renders any of the 62 dossiers (facts from `Japan.csv` with estimate hedging, `axes`-derived archetype/consent/instigator tags, a similar-cases mini-map of first-degree Part 2 neighbours + computed second-degree, plug-in content slots). Clicking a node/cluster/legend chip/finder result navigates the onion; per-layer `requestAnimationFrame` loops are cancelled and restarted on re-render so they don't stack.
- **Live finder.** The fill-in-the-blank finder now **computes in the browser** instead of showing a baked result: it builds a partial feature vector from the selections and replays `precedent_engine.py`'s `pair_score` weighting (`DATA.weights` over each node's `features`) to rank all 62 cases, updating on any change. Verified against a Python mirror of the scoring — e.g. hostile/contested + competing bid + PE sponsor + price bump ranks Fuji Soft (4.50), Benefit One (4.00), C&F Logistics (4.00), Toyo Construction (3.50), Makino (3.00), matching the engine's semantics. Still explicitly a precedent index, not a prediction.
- **`viewer/layout.json` (new, committed).** The hand-tuned node arrangement (regionally grouped by archetype, collision-relaxed) extracted so the map stays visually stable across rebuilds; positions only, radii/centroids are recomputed at build time.
- Verified: `python viewer/build.py` builds 62 nodes / 182 edges / 7 clusters cleanly; the embedded app script passes `node --check`; centroids cover all 62; every node carries features/axes/facts/neighbours.

## 2026-07-09 (3) -- Part 3: design mockup v6 (Layer 1 = explorable map)

Reworked the landing map from a static display into a navigable surface, per maintainer direction ("navigation across relations, not just a display").

- **Fits any window.** The constellation now uses `preserveAspectRatio="xMidYMid meet"` and a JS-computed stage height (`min(width-derived height, viewport - chrome)`), so the whole map is always fully visible and correctly scaled regardless of window dimensions — nothing hides below the fold. (This also let the scroll choreography drop the scale-down beat from v5.)
- **No on-map cluster labels.** Removed the persistent floating cluster names (naming is covered by the legend on the second scroll). Instead, **hovering a cluster** (via a transparent per-cluster hit-zone behind its nodes) reveals a floating cluster-name tag and spotlights that cluster's nodes; legend-chip hover does the same.
- **Pan / zoom exploration.** Drag to pan, double-click / pinch / on-screen ＋−⟲ to zoom (wheel is deliberately left to the page so it still drives the scroll choreography). As you zoom past ~1.5×, every node reveals its company name, so the map becomes readable at depth. Pan is clamped to keep the map in view; on touch, one-finger drag only pans once zoomed in, so a rest-state swipe still scrolls the page.
- **Objective 1.** Merged "Show me cases with these characteristics:" onto the end of the preceding sentence (no separate paragraph); fact buttons unchanged.

This supersedes v5 as the approved design reference for the build.

## 2026-07-09 (2) -- Part 3: design mockup v5 (final pre-build revision)

Last mockup round before the production build. Also folds in a within-session fix: v4's floating-animation loops self-invoked with an undefined timestamp, computing `NaN` node positions so the Layer 1 and Layer 2 maps rendered blank — both loops now start via `requestAnimationFrame` so the first frame gets a real timestamp.

- **Layer 1.** Reverted the monogram emblems back to plain nodes (consistent with the rest), but the ten mega-deals keep their **name labels** below the node. The landing scroll choreography's final beat now **scales the map down and settles it slightly lower** (`transform-origin` 42%) as the CTAs rise, so the lower clusters that were previously below the fold come into view; the dim overlay was softened (0.5) so the map stays legible behind the calls-to-action. Overlay stays inside the (scaling) map; the CTAs were lifted out of the map container so they don't scale or dim.
- **Objective 1.** Moved "It looks like there is / isn't a competing bid" out of the main sentence into **Advanced options**, where it now flows together with the instigator and lock-up inputs.

This mockup is the approved design reference for the full build.

## 2026-07-09 -- Part 3: design mockup v4 (design-elevation pass, GSAP)

Design-led polish of `viewer/design/mockup_japanese_minimal.html` per maintainer feedback, bringing in GSAP for choreography. GSAP loads from CDN with a **graceful fallback**: if it fails to load, every element stays visible/usable (only the scroll choreography and scroll-reveals are lost). Still a mockup; production site not built.

- **Layer 1.** (1) The 10 mega-deal nodes now render as **circular monogram emblems** — a colored ring + the company's serif initials on a washi fill. These are an **IP-safe placeholder for licensed brand logos**, which we cannot reproduce; sourcing/licensing real logo assets is logged as a future task in `To-do.md` and `Case_Content_Spec.md`. (2) Cluster names are lighter/compact and, when one is spotlighted, the others fade rather than persist. (3/4/6) A **GSAP ScrollTrigger scene** pins the landing and choreographs three beats as the user scrolls: the corner "JAPAN SPECIAL SITUATIONS / 2023 JAN — 2026 JUN" marquee (top-right, beside the vertical kanji) hands off to the cluster legend (hover-to-spotlight) in the same corner, then the map dims and the two CTAs rise centered. (5) Display renames: "Cross-border outbound" → "Cross-border", "Parent-subsidiary / carve-out" → "Parent-subsidiary" (display only; underlying `category_group` values unchanged).
- **Objective 1 finder.** Dropdowns turn red on *any* active engagement, including re-selecting the shown default (pointer-engage + change/blur logic, since native `change` doesn't fire on a same-value re-pick). First line of every sentence paragraph is indented (fact buttons are not). Results list tightened to `max-width:680px` to close the left/right whitespace, and the "precedent index, not a prediction" disclaimer moved **above** the list.
- **Layer 2 & 3.** Both maps now reuse each node's **real landing-map coordinates** (via a fit-to-viewport transform) instead of a fresh layout, so they read as *zooming into* the landing constellation rather than a reconstruction; Layer 2 keeps the floating motion. On Layer 3, the 10 second-degree cases now connect to the **first-degree node they hang off** (their `via`, computed as the highest-scoring edge into the inner five) rather than all pointing at the center.
- **Layer 3 testimonies** reveal **one at a time on scroll** (GSAP), down from two-up.
- **User guide** is now a proper slide-over drawer (scrim + panel, Escape/scrim-to-close) with a placeholder 3-step walkthrough, replacing the forced anchor link.

## 2026-07-08 (2) -- Part 3: design mockup v3 (second revision round)

Second pass on `viewer/design/mockup_japanese_minimal.html` per maintainer feedback on v2:

- **Layer 1.** The constellation now drifts continuously (per-node sinusoidal motion via `requestAnimationFrame`) for a living, multi-dimensional feel; a soft radial vignette adds depth. Cluster names moved from faded in-SVG text to crisp HTML pills positioned over each cluster centroid (always legible, never obstructed by nodes) that spotlight their cluster on hover and open the cluster view on click. Legend chips dropped the case counts and now spotlight their cluster on hover. The two calls-to-action are vertically stacked.
- **Objective 1 finder.** "Show me cases with these characteristics:" is now its own indented paragraph; dropdowns render grey until chosen, then turn red (default grey selection = "activist campaign / any / isn't"). "Strategic tender offer" renamed "strategic tender offer / TOB". Removed the TOB and MBO fact pills (already covered by the deal-type dropdown); "Activist involved" renamed "Activists in the register". Advanced options' instigator input is now a dropdown (not buttons) and the section header is just "Advanced options —".
- **Layer 2 cluster.** Relaid out as map-on-top / playbook-below (was side-by-side). Nodes use a wide, evenly-spaced horizontal arrangement with alternating above/below labels so floating never causes label overlap. The resolution table adds a Completed/Failed status tag after each ticker.
- **Layer 3 dossier.** "Similar cases" mini-map redrawn for full label legibility: the 5 nearest precedents sit bright and labeled (name + ticker) in an inner ring around Fuji Soft, with the next 10 second-degree cases (`fuji_second`, computed as neighbours-of-neighbours by best edge score) dimmed in an outer ring — dimmed nodes brighten and reveal their detail on hover.

## 2026-07-08 -- Part 3: design mockup v2 (maintainer-directed revision)

Reworked `viewer/design/mockup_japanese_minimal.html` per detailed maintainer feedback on v1. The mockup is now an interactive prototype (real behaviors, not just static screens); the production site is still not built. Changes:

- **Global.** Every user-facing `JP-###` id replaced by ticker in `xxxx JP Equity` format (from `target_ticker_code`; foreign/unlisted targets fall back to name). Added a persistent light/dark toggle (sun/moon, top-right, `localStorage`, applies to all screens) with a second validated categorical palette for the dark washi/sumi surface (dataviz checker: all checks pass on both surfaces). Added minimal on-screen navigation hints, especially on the landing map.
- **Shared map behavior (all three maps).** A reusable "gravitational" hover: the hovered node (or cluster) scales up and its links stay lit while every other node dims to the background, plus a detail bubble near the cursor. Baked node positions relaxed with a collision pass (min gap ~7px) so no labels overlap. Click a node → its dossier; click a cluster label → the cluster view.
- **Layer 1 (landing).** Dropped the working title/description (to be added later); kicker now reads "Japan Special Situations · 2023 Jan — 2026 Jun". Nodes enlarged (map was too sparse); only the ten biggest deals are labeled; cluster names float in faded serif near each cluster's centroid and sharpen on hover. Below the map: just the "describe a deal" primary action + a placeholder "user guide" link (the guide itself is a later to-do).
- **Layer 2 (finder / objective 1).** Reworked into a fill-in-the-blank sentence with inline dropdowns ("This is a ___ deal in the ___ sector. It looks like there ___ a competing bid…"), fact pills, and a collapsible **Advanced options** panel exposing the Part 2 axis inputs (instigator identity, lock-up certainty). Submit scrolls to results (no page swap); results and the "precedent index, not a prediction" disclaimer retained.
- **Layer 2 (cluster view).** Nodes now float gently and are spaced to avoid label collisions; hover shows a one-sentence "what this case was + who bid" line; the resolution write-ups in the playbook table were rewritten from broken fragments into concise 3-sentence summaries (faithful to the source `final_resolution_mechanism_raw`/`key_debate_points_raw` fields, no new facts) and now carry each target's ticker; recurring-debate chips are properly title-cased.
- **Layer 3 (dossier).** Name now sits beside its ticker and announcement date, followed immediately by tags (sector, deal type, deal status, documentation status) and a one-line party blurb, then the facts table. "Nearest precedents" renamed **Similar cases** and rendered as a live mini-map (Fuji Soft at center, its 5 nearest precedents around it) — hovering a node gives a 3-sentence similarity rationale + match score, clicking jumps to that dossier. Testimonies/context moved to the very bottom as the final section (two cards at a time), added to the content plug-in spec as a new `testimonies` frontmatter type.
- Added a note to `docs/Case_Content_Spec.md`: files are keyed by `deal_id` but the UI always displays name + ticker, never the id.

## 2026-07-07 (2) -- Part 3 kickoff: design mockup + case-content plug-in spec

Starts Part 3 (interactive site) with design-before-code, per maintainer direction. No production site code written yet.

- **Added `viewer/design/mockup_japanese_minimal.html`** -- a static, self-contained design mockup of the chosen "Japanese minimal" direction (washi/ink/vermilion, serif headings; chosen over light-editorial and evolved-dark alternatives). Shows all four screens of the three-layer "onion": calm landing page (constellation only + one action), find-precedent form, zoomed cluster view with scenario playbook, and full case dossier. Built on **real data, not lorem ipsum**: baked spring-layout positions for all 62 nodes/182 edges, a genuinely-computed top-5 finder result under the current Part 2 weights (hostile-TOB demo query returns Fuji Soft / Benefit One / C&F / Toyo Construction / Makino), the real contested-hostile cluster (6 cases, true 4/2 outcome split, real resolution one-liners), and JP-026 Fuji Soft as the dossier example. The 6-hue categorical node palette (+ recessive gray for "Other") was validated with the dataviz palette checker against the washi surface (lightness band, chroma floor, CVD separation, contrast -- all pass).
- **Added `docs/Case_Content_Spec.md` + `content/cases/` scaffold.** Maintainer decision: executive summaries and source links are being produced **separately** -- Part 3 writes none of either, and nothing is auto-generated. The spec defines the plug-in contract (one `content/cases/JP-XXX.md` per case, YAML frontmatter with typed/verified links + Markdown summary body, hard build-time validation, honest "pending" placeholder rendering when a file doesn't exist) so content mounts later without touching site code.
- Dossier-page mockup surfaces existing dataset caveats in the design itself: verification-confidence badge, estimate markers, and the permanent "precedent index, not a prediction" line under finder results.

## 2026-07-07 -- Part 2 follow-up: community-detection tuning, redundancy consolidation

Resolves the two open items from 2026-07-06 (5)'s to-do list; see `docs/To-do.md` 2026-07-07 for the full investigation and numbers.

- **Root-caused the degenerate k-clique community detection.** It was both causes at once: `MIN_SCORE=1.5` (reused from the viewer's edge-trimming logic) let 79.7% of all 1,891 pairs "connect," and k-clique percolation on this corpus's feature-sharing structure always converges to one dominant community regardless of threshold (verified via a grid search to threshold=9.0) -- a few small, genuinely rare-feature-driven satellite communities exist, but there's no clean way to subdivide the "ordinary deal" majority the same way. Added a dedicated `COMMUNITY_MIN_SCORE=7.0` constant in `precedent_engine.py`, independent of the viewer's `MIN_SCORE=1.5` (unchanged). Now produces 5 communities (sizes [16,3,3,3,3], 26/62 nodes covered) instead of 1 giant blob.
- **Reviewed all 9 flagged high-Jaccard pairs using lift-over-independence, not just raw Jaccard.** Lift separates genuine correlation from the artifact of two common features both being common (which inflates Jaccard regardless of any real relationship). Only 3 of 9 pairs showed real above-base-rate correlation; the rest, including the highest-Jaccard pair (`timeline_post_meti_2023_guideline` x `timeline_post_tse_reform_2023`, 0.93), were ~1.0-1.09x lift -- essentially base-rate, not redundancy.
- **Added `data/Japan.csv` columns (137 -> 139)**: `has_dual_antitrust_review` (`has_JFTC` AND `has_DOJ_FTC_HSR`, both true; 39/62) replacing those two as separately-scored features (genuine 1.37-1.54x correlation; `has_JFTC`'s true set was an exact subset of `has_DOJ_FTC_HSR`'s), weight 0.5 (down from a combined 1.1). `timeline_post_2023_reforms` (`timeline_post_meti_2023_guideline` AND `timeline_post_tse_reform_2023`, both true; 53/62) replacing those two, weight 0.1 -- consolidated despite lift showing this was mostly a base-rate artifact, per an explicit maintainer caution call rather than the statistical evidence alone. All four original columns remain in `Japan.csv` as individual descriptive fields; only their scoring in `pair_score()` was consolidated. `structure_has_TOB` was deliberately left unconsolidated and unchanged (0.5) -- it's a distinct deal-structure fact, not a regulatory one, even though it correlates with the antitrust flags.
- **Re-ran the Jaccard check post-consolidation**: down from 9 flagged pairs to 2 (both already reviewed/accepted -- `structure_has_TOB`x`has_dual_antitrust_review` kept deliberately, `toehold_present_flag`x`timeline_post_2023_reforms` already assessed as base-rate).
- **`timeline_post_fiea_2026_amendment`**: reviewed and confirmed no action -- stays at weight 0.1, contributing 0 to every score until the corpus includes deals announced after 2026-05-01.

## 2026-07-06 (5) -- Part 2: axis-tag clustering, community-detection cross-check, new weighted features

Implements the core Part 2 redesign called for in `Claude.md`'s product vision: precedents should group into **overlapping clusters**, feeding a future descriptive scenario-playbook UI (Part 3, not built yet). This session builds the data layer for that; no UI consumes it yet.

- **Added `data/Japan.csv` columns (134 -> 137)**: `multi_jurisdiction` (`num_regulators >= 2`), `notes_flags_precedent_setting` (keyword flag on `notes_raw`: "first"/"template"/"precedent"/"proof-of-concept"/"landmark"/"signature" -- 7/62 true), `price_was_bumped_bool` (a clean boolean fix: the source `price_was_bumped` column is an object column mixing the strings `'True'`/`'False'`/`'Unclear/TBV'` and `NaN`, and the existing `bool(value)` pattern used elsewhere in `precedent_engine.py` would incorrectly treat `'Unclear/TBV'` and `NaN`-as-string as truthy; `price_was_bumped_bool` is `True` only where the source is the literal string `'True'` -- 7/62 true, matching the ~7/62 estimate). All three mechanically derived, no new facts. Documented in `docs/Japan_User_Guide.md`.
- **Added `src/cluster/axes.py`** -- a new module, factored out of (not copy-pasted from) `notebooks/exploratory_analysis.py`'s Lenses 1/2/3/4/5/6/8. Exposes `compute_axes(row, activist_columns)` returning 7 descriptive per-case axes (consent, price-discovery type, regulatory friction, precedent-value text, instigator identity, lock-up signal count, activist signature), plus a ported `jaccard`/`top_cooccurring_pairs` helper for double-counting checks. These axes are the intended multi-membership clustering signal -- a case naturally carries several at once.
- **Extended `src/cluster/precedent_engine.py`**:
  - 7 new `BOOLEAN_FEATURES` entries, weights grounded in each feature's real true-rate in the 62-row corpus (not guessed) -- see `docs/Architecture.md` for the full table and rationale. `timeline_post_fiea_2026_amendment` contributes 0 to every score today (0/62 true; the amendment postdates this corpus) and will only start mattering once the corpus grows past 2026-05-01.
  - Deliberately did **not** score `instigator_type`/`lockup_signal_count`/`activist_signature` as features -- each recombines columns already scored independently, so scoring the composite too would double-count.
  - Attached each node's `axes` dict (from the new module) and a `short_name` (compact display label -- strips parentheticals and trailing corporate-suffix stopwords from `target_full_name`, falls back to the first 3 words if empty).
  - Added a secondary overlapping-community cross-check via `networkx.algorithms.community.k_clique_communities` on a full (untrimmed) pairwise graph at the existing `MIN_SCORE=1.5`. At `k=3` this is **degenerate on the current 62-case corpus** -- a single community containing all 62 nodes -- so every node's `community_ids` is currently `[0]` and not yet informative; kept as-is per the reviewed plan (try k=3 first, report if degenerate rather than silently re-tuning) and flagged in `To-do.md`.
  - Added `networkx` to `requirements.txt`.
- **Viewer**: changed the always-visible node label in `viewer/Japan_Precedent_Constellation.html` from `d.id` to `d.short_name` -- `deal_id` remains visible in the detail panel and search, unchanged.
- **Ran an empirical Jaccard double-counting check** (Task 4 of this session) across the full boolean feature set now scored by `precedent_engine.py` (all pre-existing `BOOLEAN_FEATURES`/`activist_*` plus the 7 new features this session), min support 3. Beyond the already-acknowledged pairs, found several **unexpected** high-overlap (Jaccard > 0.6) pairs not previously flagged: `timeline_post_meti_2023_guideline` x `timeline_post_tse_reform_2023` (0.93), `has_JFTC` x `has_DOJ_FTC_HSR` (0.89), `structure_has_TOB` x `has_JFTC` (0.80), `has_DOJ_FTC_HSR` x `timeline_post_tse_reform_2023` (0.71), `structure_has_TOB` x `has_DOJ_FTC_HSR` (0.71), `toehold_present_flag` x `timeline_post_tse_reform_2023` (0.68), `has_DOJ_FTC_HSR` x `timeline_post_meti_2023_guideline` (0.67), `toehold_present_flag` x `timeline_post_meti_2023_guideline` (0.67), `has_JFTC` x `timeline_post_tse_reform_2023` (0.63). No weights were changed in response -- weights are a maintainer judgment call per `Claude.md`; logged for review in `docs/To-do.md`.
- **Caught and fixed a bug during post-implementation review**: the viewer's `const DATA = {...}` object is hand-embedded JavaScript (see `Architecture.md`'s "known gap"), not loaded from `output/precedent_graph_data.json` at runtime. The node-label fix (`d.id` -> `d.short_name`) was applied to the rendering code, but the embedded `DATA` blob itself was never re-synced after `precedent_engine.py` was re-run -- so every node's `short_name` would have rendered as `undefined` in the actual browser. Fixed by re-embedding the freshly regenerated JSON into the HTML file (same manual copy-paste process already documented as a Part 3 gap). Verified post-fix: the embedded `DATA.nodes[0]` now carries `short_name`/`axes`/`community_ids`, matching `output/precedent_graph_data.json`.

## 2026-07-06 (4)

- **Flagged a merge-coordination note in `To-do.md`** ("2026-07-06 (3) -- merge coordination"): the regulatory-timeline and advisor-data edits below (entries "2026-07-06" and "2026-07-06 (2)") were made while a separate scraping effort was concurrently modifying `data/Japan_Master.csv` in the same working tree. No conflict has occurred yet since neither side has committed, but `To-do.md` now lists exactly which columns/cells this session touched so the two efforts can be reconciled column-by-column rather than via a raw line diff, which is unreadable on a wide CSV once columns shift.

## 2026-07-06 (3)

- **Added a pilot batch of M&A financial-advisor data** to `data/Japan_Master.csv` (`Financial Advisor - Target`, `Financial Advisor - Acquirer`, `Target IPO Advisor` columns, all 62 rows, default `TBV`) and propagated to `data/Japan.csv` (`financial_advisor_target_raw`, `financial_advisor_acquirer_raw`, `target_ipo_advisor_raw`; 131 -> 134 columns). Researched and verified 7 deals (JP-001 Toshiba/JIP, JP-002 JSR/JIC, JP-006 Benefit One/Dai-ichi Life, JP-009 Outsourcing/Bain, JP-010 Shinko Electric/JIC-DNP-Mitsui, JP-011 U.S. Steel/Nippon Steel) from public tender-offer filings and press releases; JP-003 (Shinsei Bank/SBI) and JP-004 (Takisawa/Nidec) were attempted but no confident public source was found, left `TBV`. This is an explicit, one-time exception to the project's "no new facts" rule (see `Claude.md` #2) -- new research was added at the `Japan_Master.csv` level specifically so `Japan.csv` still mechanically derives from the master file, preserving the audit trail.
- **Caught and corrected a hallucinated finding during this research**: an initial WebFetch summary of Toshiba's own tender-offer PDF claimed Goldman Sachs + Lazard advised Toshiba and Nomura + Mizuho advised JIP -- backwards and materially wrong. Re-reading the primary document directly (not through a summarizing fetch) showed the correct advisors: Nomura Securities/Mizuho Securities/JPMorgan Securities Japan for Toshiba's management team, UBS Securities Japan independently for the Board/Special Committee, and Crosspoint Advisors Co., Ltd. for JIP. Kept as a reminder to verify AI-summarized source documents against the primary text before trusting them for this corpus.
- **Attempted IPO-advisor (lead underwriter) research and found it not viable via web search for this pilot**: searches for Shinsei Bank's 2004 IPO and Benefit One's / Outsourcing Inc.'s IPOs returned conflicting listing years and underwriter claims from low-quality sources. `target_ipo_advisor_raw` is left `TBV` for all 62 rows pending a better sourcing approach (e.g. EDINET securities registration statements) -- tracked in `To-do.md`.

## 2026-07-06 (2)

- **Added `docs/Regulatory_Timeline_Appendix.md`.** Substantive background (what each rule is, what changed, why it matters for this corpus, sourced links) for the three regulatory regimes behind the `timeline_post_*` columns added earlier today -- the CSV columns are the mechanical flags, this doc is the "why it matters" reference. Verified all three effective dates against primary/law-firm sources before writing (METI 2023-08-31, TSE 2023-03-31, FIEA amendment 2026-05-01 given Diet passage 2024-05-15).

## 2026-07-06

- **Added regulatory-timeline overlay columns to `data/Japan.csv`** (128 -> 131 columns): `timeline_post_meti_2023_guideline`, `timeline_post_tse_reform_2023`, `timeline_post_fiea_2026_amendment`. Each is a mechanical boolean derived from comparing `date_announced` to a rule's real-world effective date (METI Corporate Takeover Guidelines 2023-08-31, TSE cost-of-capital/PBR reform request 2023-03-31, FIEA mandatory-TOB amendment 2026-05-01) -- no new facts introduced. Distinct from the pre-existing `mentions_METI_guidelines`/`mentions_TSE_reform`/`mentions_FIEA` text-keyword flags. Documented in `Japan_User_Guide.md` and `Architecture.md`. Not yet wired into `precedent_engine.py`'s similarity score.
- **Decided against a `situation_id` linking field** for multi-bid contests / repeat approaches (e.g. Seven & i/Couche-Tard vs. York/Bain, Nidec/Makino vs. MBK/Makino). Each approach stays its own row/node; the pre-existing `recurring_acquirer_flag` is the intended signal that a case is a repeat/serial approach and should be read as a marker of a highly-correlated precedent pair. No schema change made for this item.
- **Cleared four other `To-do.md` items via maintainer decision, no code changes**: keep high-cardinality `*_raw` columns as-is (lookup/detail use only); keep small-n regulator-flag cuts (`has_METI` n=4, `has_CFIUS` n=2) un-cited pending corpus growth/verification; defer `output/precedent_graph_data.json` -> viewer sync until Part 3. `precedent_established` remains open, deferred pending a planned NLP pass over `notes_raw`/`key_debate_points_raw`.

## 2026-07-04

- **Reorganized the repo into `data/`, `src/`, `docs/`, `output/`, `viewer/`.** Previously everything lived flat in the working directory. Moving to a conventional layout was a prerequisite for Part 1 (adding a real exploration script) and for making the repo presentable as an open-source project.
- **Added `src/explore.py` (Part 1 -- data exploration & visualization).** Generates a data-quality report and a set of charts directly from `data/Japan.csv`, using column-discovery-by-naming-convention so it keeps working if the CSV's schema changes. See `Architecture.md` for design details.
- **Fixed `src/cluster/precedent_engine.py`'s file paths after the move.** It previously read/wrote `Japan.csv` / `precedent_graph_data.json` relative to the working directory; now resolves `data/Japan.csv` and `output/precedent_graph_data.json` relative to the repo root via `pathlib.Path(__file__)`, so it runs correctly regardless of caller cwd. No scoring/weight logic was changed.
- **Moved `Japan_Precedent_Constellation.html` to `viewer/`.** No content changes -- its graph data is embedded inline in the file, so the move doesn't affect it.
- **Added the 4 running docs** (`Architecture.md`, `Changelog.md`, `To-do.md`, `Roadmap.md`) to track structure decisions, changes, open manual-verification items, and the 3-part roadmap across sessions.
- **Initialized git.** The project previously had no version control.
- **Clarified the product vision in `Claude.md`.** Added an explicit "how the tool is meant to be used" section: query a new deal's facts -> retrieve comparable precedents -> group into overlapping clusters (a case can belong to more than one) -> display a descriptive "scenario playbook" per cluster (what happened across comparable precedents, not a predictive probability for the new deal). Also updated `Claude.md`'s file table to match the new repo layout and pointed its "recommended next steps" at `docs/To-do.md`/`docs/Roadmap.md` instead of duplicating them.
- **Added `notebooks/exploratory_analysis.py` (Part 1, Colab-openable).** Deeper exploration aimed specifically at informing Part 2's similarity/clustering design: feature discriminativeness/rarity, feature co-occurrence/redundancy, pairwise feature-comparability coverage, diagnostics on the existing similarity graph, and seven interpretive lenses (consent, price-discovery, regulatory friction, precedent-value, instigator-identity, deal-certainty, activist signature) -- all grounded in existing columns, no new facts. Outcome cross-tabs kept as descriptive context only (no significance testing, `Other/Unclear` never folded into a rate denominator). See `Architecture.md` for full section-by-section detail.
- **Tried and reverted a citation-network lens** in the same notebook -- mining case notes for explicit mentions of other cases' company names produced mostly false positives (generic conglomerate-name prefixes and repeat-sponsor overlap, not genuine cross-references), so it was dropped rather than shipped. See `Roadmap.md` Part 1.
